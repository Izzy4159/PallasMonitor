flask>=2.3.0
psutil>=5.9.0
pywebview>=4.4.1

# Optional — install for full hardware sensor support:
# nvidia-ml-py   — NVIDIA GPU via pynvml
# GPUtil         — GPU fallback
# wmi            — Windows sensor data (fans, CPU temp via LibreHardwareMonitor)
# pywin32        — required by wmi on Windows
